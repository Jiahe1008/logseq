### 1. ModR/M 字节与 3 位寄存器字段

在 x86 指令集中，操作数寄存器通常由 `ModR/M` 字节中的 3 位字段（`reg` 或 `r/m`）来指定。3 个二进制位只能表示 $2^3 = 8$ 个不同的值（0-7）。

在 32 位模式（或不带 REX 前缀的 64 位模式）下，对于 **8位操作数**，这 8 个值的映射关系如下：

请注意，在这个传统模式下，我们无法访问 SP、BP、SI、DI 的低 8 位（即 SPL, BPL, SIL, DIL），因为编码 4-7 被分配给了高字节寄存器（AH-BH）。 为什么这里无法访问？不是有8位16位32位64位那么多寄存器，8位够吗？
- ###  **2. 实模式下的分段模型（8086）**
- ####  **2.1 为什么需要分段？**
  8086有20位地址线（1MB），但寄存器只有16位（64KB）。
-
- 协处理器
	-