- $$C = B\times E\times S$$
- 每组的每行含有1个有效位，t个标记位，$$B = 2^b$$ 个缓存块字节。可以用结构$$(S,E,B,m)$$表示高速缓存的结构。
- K级缓存存储的时K+1级缓存的子集
- 缓存地址共有m位，则m = t(标记位）+ s（组索引）+b（块偏移），其中$$S = 2^s$$
	- 直接映射高速缓存
	  id:: 69369aad-5075-4ec8-8b61-7111182fe313
	- 组相联高速缓存
- ### 写
-
- 对于[[结构]]，其中内存是连续的。（[[练习6.18]]）缓存读取的每个块必须存储整个结构内存
-